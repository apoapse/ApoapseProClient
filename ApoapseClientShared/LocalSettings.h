#pragma once
#include "SQLQuery.h"

// #TODO complete system
/*
class LocalSettings
{
	struct Value
	{
		SQLValue value;
		std::string defautValue;
		bool onDb = false;
	};

	std::map<std::string, Value> m_values;

public:
	LocalSettings();
	void RegisterSettings();
	
	template <typename T>
	T GetValue(const std::string& settingName) const
	{
		m_values.at(settingName).value;
	}

	template <typename T>
	void SaveValue(T value) const
	{

	}

private:
	void ReadValuesFromDb();
	void RegisterValue(const std::string& name, std::string defaultValue);
};*/